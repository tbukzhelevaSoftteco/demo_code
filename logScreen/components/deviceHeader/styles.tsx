import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainerStyle: {
    alignItems: 'center',
  },
  textStyle: {
    fontSize: 18,
    fontWeight: 'bold',
  },
});

export default styles;
